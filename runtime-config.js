// Optional custom clock override. The public HTTPS clock is used when this is empty.
// The Pages workflow replaces it from the DRIFTER_CLOCK_URL repository variable.
window.DRIFTER_CONFIG = {
  clockEndpoint: "",
};
